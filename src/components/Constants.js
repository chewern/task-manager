//all the urls to interact with backend server

const ServerURL = "http://localhost:8000";

export const PostURL = ServerURL + "/tasks/";
export const DeleteURL = ServerURL + `/tasks/`;
export const PutURL = ServerURL + `/tasks/`;
export const GetTaskURL = ServerURL + `/tasks/`;
export const GetAllTasksURL = ServerURL + "/tasks/";
